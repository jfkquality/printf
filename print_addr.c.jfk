#include "holberton.h"

/**
 * print_addr - print void * type.
 *@str: string
 * Return: string
 */
int print_addr(void *address)
{
	int i = 0;
	int length;
	for (length = 0 ; address[length] ; ++length)
	{
		i += _putchar(address[length]);
	}
	i--;
	return (i);
}
